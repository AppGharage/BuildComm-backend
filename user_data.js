//load db configuration
//const db = require('./app/config/config.js');

//const prevQuery = "SELECT * FROM fb_users WHERE oauth_provider = '"+ oauth_provider + "' AND oauth_uid = '"+ userData->id+"'";